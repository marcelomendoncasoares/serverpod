### Model with partitionBy using multiple columns.
class: PartitionedMultiColumn
table: partitioned_multi_column
fields:
  source: String
  category: String
  value: int
partitionBy:
  method: range
  fields: source, category

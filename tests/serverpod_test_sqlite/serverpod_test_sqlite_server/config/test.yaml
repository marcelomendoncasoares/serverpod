apiServer:
  port: 0
  publicHost: localhost
  publicPort: 0
  publicScheme: http

insightsServer:
  port: 0
  publicHost: localhost
  publicPort: 0
  publicScheme: http

webServer:
  port: 0
  publicHost: localhost
  publicPort: 0
  publicScheme: http

database:
  filePath: serverpod_test_test.db

sessionLogs:
  persistentEnabled: true
  consoleEnabled: true

futureCallExecutionEnabled: false
